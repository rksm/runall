# runall

## runall

Install: `cargo install runall`

### Usage

```shell
$ runall --help
Run multiple commands in parallel.

Usage: runall [OPTIONS] [COMMANDS]...

Arguments:
  [COMMANDS]...

Options:
  -n, --names <NAMES>
  -h, --help           Print help
```
